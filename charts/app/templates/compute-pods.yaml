---
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "app.name" . }}-compute
  namespace: {{ .Values.nameSpace }}
  labels:
{{ include "app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  initContainers:
  - name: init-{{ include "app.name" . }}-compute
    image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
    command: [ "/bin/bash", "-c", "--" ]
    args:  [ "test ! -f /data/.bash_history && cd /data && tar xvzf /root/user_home.tgz ; echo 'test -d /data/app && cd /data/app && ./mg-compute.py' >> /start_container.sh ; echo 'while true; do sleep 30; done;' >> /start_container.sh ; chown -R app /data" ]
    securityContext:
      runAsUser: 0
    volumeMounts:
    - name: {{ include "app.name" . }}-compute-vol
      mountPath: /data
  containers:
  - name: {{ include "app.name" . }}-compute
    image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
    command: [ "/bin/bash", "-c", "--" ]
    args:  [ "/start_container.sh" ]
    volumeMounts:
    - name: {{ include "app.name" . }}-compute-vol
      mountPath: /data
  volumes:
  - name: {{ include "app.name" . }}-compute-vol
    persistentVolumeClaim:
      claimName: {{ include "app.name" . }}-compute-pvc
  restartPolicy: Never
